name: ableron-js with Node.js 20

on: push

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: 20
      - run: ./gradlew test --tests '*.NodeJs20Spec'
